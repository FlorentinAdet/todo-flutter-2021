class Task {
  String content;
  int id;
  bool completed;
  DateTime createdAt;

  //Constructeur
  Task(this.id, this.content, this.completed, this.createdAt) {
    // Initialization code goes here.
  }
  
}